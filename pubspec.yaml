name: text_with_highlight
description: Super lightweight widget that adds a rich text containing highlighted words/sentences.
version: 1.0.0
homepage:

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter: